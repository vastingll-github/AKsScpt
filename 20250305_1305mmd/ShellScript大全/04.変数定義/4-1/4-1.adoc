[[globalheader-container]]


# bashのシェル変数（全95個） #

[[content]]
[[content-inner]]
[[wrapper]]
[[main]]
[[main-inner]]

== bashの予約変数（全95個）

== 一覧

　以下の予約変数はシェルが設定する。（48個）

[width="100%",cols="8%,42%,50%",options="header",]
|===
|項番|シェル予約変数 |概要
|1|`BASH`
|現在実行しているhttp://d.hatena.ne.jp/keyword/bash[bash]のフルパス。

|2|`BASHOPTS`
|有効になっているシェルのオプションを示す(コロン区切り)。読込専用。`shopt -s`で`on`と表示されるものである。

|3|`BASHPID`
|現在のhttp://d.hatena.ne.jp/keyword/bash[bash]プロセスID。（$$は非更新）

|4|`BASH_ALIASES`
|http://d.hatena.ne.jp/keyword/%A5%A8%A5%A4%A5%EA%A5%A2%A5%B9[エイリアス]の内部的なリストに対応するhttp://d.hatena.ne.jp/keyword/%CF%A2%C1%DB%C7%DB%CE%F3[連想配列]変数。

|5|`BASH_ARGC`
|http://d.hatena.ne.jp/keyword/bash[bash]の現在の呼出スタックについて、フレームごとの引数の数が並んだ配列変数。

|6|`BASH_ARGV`
|http://d.hatena.ne.jp/keyword/bash[bash]の現在の呼出スタックについて、全ての引き数が並んだ配列変数。

|7|`BASH_CMDS`
|http://d.hatena.ne.jp/keyword/bash[bash]が内部に持つコマンドのハッシュテーブルに対応するhttp://d.hatena.ne.jp/keyword/%CF%A2%C1%DB%C7%DB%CE%F3[連想配列]変数。

|8|`BASH_COMMAND` |現在実行しているか実行しようとしているコマンド。

|9|`BASH_EXECUTION_STRING` |起動オプション -c で指定されたコマンド。

|10|`BASH_LINENO`
|ソースファイル中の行番号からなる配列変数。`FUNCNAME`と対応する。現在の行番号は`LINENO`。

|11|`BASH_REMATCH`
|`[[`中のhttp://d.hatena.ne.jp/keyword/%C6%F3%B9%E0%B1%E9%BB%BB%BB%D2[二項演算子]`=~`により設定される配列変数。

|12|`BASH_SOURCE` |ソースファイル名からなる配列変数。

|13|`BASH_SUBSHELL`
|サブシェルやサブシェル環境が作成されるたびに1増加。初期値`0`。

|14|`BASH_VERSINFO` |`0`: メジャー +
`1`: マイナー +
`2`:パッチレベル +
`3`:ビルドバージョン +
`4`:リリースステータス(beta1等) +
`5`:MACHTYPE の値

|15|`BASH_VERSION`
|現在実行しているhttp://d.hatena.ne.jp/keyword/bash[bash]のバージョン。

|16|`COMP_CWORD`
|現在カーソル位置がある単語の配列変数`${COMP_WORDS}`におけるインデックス。

|17|`COMP_KEY` |現在の補完関数を呼び出したキー。

|18|`COMP_LINE`
|現在のhttp://d.hatena.ne.jp/keyword/%A5%B3%A5%DE%A5%F3%A5%C9%A5%E9%A5%A4%A5%F3[コマンドライン]。

|19|`COMP_POINT`
|現在のコマンドの先頭からの相対値として与えられたカーソル位置のインデックス。

|20|`COMP_TYPE`
|補完関数を呼び出した補完のタイプに対応する整数値が設定される。 +
`<TAB>`: 通常の補完 +
`?`: 連続したタブ入力による候補リスト +
`!`: 途中まで補完した後の候補リスト +
`@`: 部分的な補完ができないときの候補リスト +
`%`: メニュー補完 (menu completion)

|21|`COMP_WORDBREAKS`
|単語補完のときにreadlineライブラリが単語分割の区切文字として扱う文字の並び。

|22|`COMP_WORDS`
|現在のhttp://d.hatena.ne.jp/keyword/%A5%B3%A5%DE%A5%F3%A5%C9%A5%E9%A5%A4%A5%F3[コマンドライン]の各単語からなる配列変数。

|23|`COPROC`
|無名のコプロセスが入出力するファイル・http://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%B9%A5%AF%A5%EA%A5%D7%A5%BF[ディスクリプタ]ーを保持する配列変数。

|24|`DIRSTACK`
|現在のhttp://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リスタックの内容を持つ配列変数。

|25|`EUID` |現在のユーザの実効ユーザID。

|26|`FUNCNAME`
|現在の呼び出しスタックにある全てのシェル関数名が入った配列変数。

|27|`GROUPS`
|現在のユーザがメンバになっているグループのリストを含んだ配列変数。

|28|`HISTCMD` |現在のコマンドの履歴番号(履歴リストにおけるインデックス)。

|29|`HOSTNAME` |現在のホスト名。(`raspberrypi`等)

|30|`HOSTTYPE`
|http://d.hatena.ne.jp/keyword/bash[bash]を実行するマシンの種類をユニークに記述する文字列。(`arm`等)

|31|`LINENO` |行番号。

|32|`MACHTYPE`
|http://d.hatena.ne.jp/keyword/bash[bash]を実行するシステムの種類（http://d.hatena.ne.jp/keyword/GNU[GNU]標準のcpu-company-system形式）(`arm-unknown-linux-gnueabihf`)

|33|`MAPFILE`
|組込コマンド`mapfile`に変数名が指定されなかったときに読み込んだテキストを保持する配列変数。

|34|`OLDPWD`
|1つ前の作業http://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リ。

|35|`OPTARG` |組込コマンド`getopts`で処理した最後のオプション引数値。

|36|`OPTIND`
|組込コマンド`getopts`で次に処理されるオプション引数インデックス。

|37|`OSTYPE`
|http://d.hatena.ne.jp/keyword/bash[bash]を実行するOSを記述する文字列。(`linux-gnueabihf`等)

|38|`PIPESTATUS`
|フォアグラウンドで最後に実行したパイプラインの各プロセスの終了ステータスのリストを含む配列変数。

|39|`PPID` |そのシェルの親のプロセスID。

|40|`PWD`
|現在の作業http://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リ。

|41|`RANDOM` |`0`〜`32767`までのランダムな整数。

|42|`READLINE_LINE` |readlineの編集バッファ内容。`bind -x`で使う。

|43|`READLINE_POINT`
|readlineの編集バッファでの挿入ポイント位置。`bind -x`で使う。

|44|`REPLY`
|組込コマンド`read`に引き数が与えられなかったときに読み込まれた行。

|45|`SECONDS` |シェルが起動されてからの秒数。

|46|`SHELLOPTS` |有効になっているシェルのオプションを示す。（コロン区切り）
リスト中の単語は組込コマンド`set -o`に対応。

|47|`SHLVL`
|http://d.hatena.ne.jp/keyword/bash[bash]が起動するときにhttp://d.hatena.ne.jp/keyword/%B4%C4%B6%AD%CA%D1%BF%F4[環境変数]で渡された値から1増やした値。

|48|`UID` |現在のユーザのユーザID。
|===

　以下の変数はシェルが使用する。（47個）

[width="100%",cols="50%,50%",options="header",]
|===
|シェル変数 |概要
__|`BASH_ENV`
|http://d.hatena.ne.jp/keyword/bash[bash]がhttp://d.hatena.ne.jp/keyword/%A5%B7%A5%A7%A5%EB%A5%B9%A5%AF%A5%EA%A5%D7%A5%C8[シェルスクリプト]を実行するときにこの値が設定されている場合、この値は(~/.bashrc
のように)シェルを初期化するコマンドが書かれているファイル名と解釈される。

|`BASH_XTRACEFD`
|有効なファイル・http://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%B9%A5%AF%A5%EA%A5%D7%A5%BF[ディスクリプタ]ー(fd)に対応する整数をセットするとhttp://d.hatena.ne.jp/keyword/bash[bash]は`set -x`が設定されたときに、生成されたトレース出力を
そのfdに出力する。`BASH_XTRACEFD`が`unset`されるか新しい値が代入されるとfdは閉じる。

|`CDPATH` |`cd`コマンドの検索パス。コロン区切り。

|`COLUMNS`
|組込コマンド`select`によって選択されたリストを表示する際の端末幅の決定に用いられる。

|`COMPREPLY`
|http://d.hatena.ne.jp/keyword/bash[bash]が可能な補完候補を読み込む配列変数。

|`EMACS`
|http://d.hatena.ne.jp/keyword/bash[bash]が起動したときにこのhttp://d.hatena.ne.jp/keyword/%B4%C4%B6%AD%CA%D1%BF%F4[環境変数]が設定されて値が`t`になっていると行編集を無効化する。

|`ENV`
|`BASH_ENV`と類似の変数でhttp://d.hatena.ne.jp/keyword/POSIX[POSIX]モードで使われる。

|`FCEDIT` |組み込みコマンド`fc`が使うデフォルトのエディタ。

|`FIGNORE`
|ファイル名補完を行う際に無視するhttp://d.hatena.ne.jp/keyword/%A5%B5%A5%D5%A5%A3%A5%C3%A5%AF%A5%B9[サフィックス]（コロン区切り）。`.o:~`等。

|`FUNCNEST`
|`0`より大きい数値を設定すると関数呼び出しを何重まで許すかの最大レベルを決める。

|`GLOBIGNORE`
|パス名展開で無視するファイル名の集合を定義するパターン（コロン区切り）

|`HISTCONTROL` |履歴リストに入れるコマンドを制御する値（コロン区切り）

|`HISTFILE`
|コマンド履歴が保存されるファイルの名前。デフォルト値`~/.bash_history`。

|`HISTFILESIZE` |履歴ファイルに保持する履歴の最大数。

|`HISTIGNORE`
|どのコマンド行を履歴リストに保存するかを決めるために使うパターン（コロン区切り）

|`HISTSIZE` |コマンド履歴に記憶するコマンドの数。 デフォルト`500`。

|`HISTTIMEFORMAT`
|この変数に空でない値が設定されると組込コマンド`history`で履歴エントリを表示するときにタイムスタンプを表示するための`strftime(3)`の書式文字列
として使われる。

|`HOME`
|現在のユーザのホームhttp://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リ。

|`HOSTFILE`
|シェルがホスト名を補完する必要があるときに読み込むファイルの名前を示す。

|`IFS` |内部フィールド区切文字(Internal Field
Separator)。デフォルト値`<空白><タブ><改行>`。

|`IGNOREEOF` |単独で入力された EOF
文字を受け取ったときの対話シェルの動作を制御する。

|`INPUTRC`
|readlineの起動ファイルのファイル名。デフォルト値`~/.inputrc`を上書きする。

|`LANG`
|http://d.hatena.ne.jp/keyword/%A5%ED%A5%B1%A1%BC%A5%EB[ロケール]設定。（`LC_`より優先度低）

|`LC_ALL`
|http://d.hatena.ne.jp/keyword/%A5%ED%A5%B1%A1%BC%A5%EB[ロケール]設定。（優先度最高）

|`LC_COLLATE`
|パス名展開の結果をソートするときに使用される照合順序と、パス名展開とパターンマッチングにおける範囲表現、等値クラス、照合順序の動作を決定する。

|`LC_CTYPE`
|パス名展開とパターンマッチングにおける文字解釈と文字クラスに含まれる文字を決める。

|`LC_MESSAGES`
|`$`の後に続くダブルクォートされた文字列の翻訳に使うhttp://d.hatena.ne.jp/keyword/%A5%ED%A5%B1%A1%BC%A5%EB[ロケール]を決める。

|`LC_NUMERIC`
|数字のフォーマットに使用するhttp://d.hatena.ne.jp/keyword/%A5%ED%A5%B1%A1%BC%A5%EB[ロケール]カテゴリを決める。

|`LINES`
|組込コマンド`select`によって選択されたリストを表示する際の行数の決定に用いる。

|`MAIL`
|このパラメータにファイル名またはhttp://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リ名が設定されており、かつ変数`MAILPATH`が設定されていなければhttp://d.hatena.ne.jp/keyword/bash[bash]は指定されたファイルまたはMaildier形式のhttp://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リへのメールの到着をユーザに通知します。

|`MAILCHECK`
|http://d.hatena.ne.jp/keyword/bash[bash]がメールをチェックする頻度を秒数で指定。デフォルトは`60`秒。

|`MAILPATH` |メールのチェックに使うファイル名（コロン区切り）。
特定ファイルにメールが到着したときの出力メッセージは`?`を使ってファイル名をメッセージから区切ることで指定可。メッセージのテキスト中で使われたとき`$_`は現在のメールファイルの名前になる。`MAILPATH='/var/mail/bfox?"You have mail":~/shell-mail?"$_ has mail!"'`

|`OPTERR`
|`1`が設定されている場合http://d.hatena.ne.jp/keyword/bash[bash]は組込コマンド`getopts`を使って生成したエラーメッセージを表示する。

|`PATH` |コマンドの検索パス。（コロン区切り）

|`POSIXLY_CORRECT`
|http://d.hatena.ne.jp/keyword/bash[bash]が起動したときにこのhttp://d.hatena.ne.jp/keyword/%B4%C4%B6%AD%CA%D1%BF%F4[環境変数]が設定されていると、起動オプション`--posix`を指定したときと同じくhttp://d.hatena.ne.jp/keyword/posix[posix]モードになる。
シェルの実行中にこの変数が設定されるとhttp://d.hatena.ne.jp/keyword/bash[bash]は`set -o posix`コマンドを実行したときと同様にhttp://d.hatena.ne.jp/keyword/posix[posix]モードが有効になる。

|`PROMPT_COMMAND`
|設定されているとプライマリプロンプトを出す前に毎回、この値がコマンドとして実行されます。

|`PROMPT_DIRTRIM`
|`0`より大きい値が設定されると、プロンプト文字列のhttp://d.hatena.ne.jp/keyword/%A5%A8%A5%B9[エス]ケープシーケンス`\w`や`\W`を展開するときにhttp://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リがパス名の最後からこの数だけ残る。

|`PS1`
|このパラメータの値は展開されてプライマリのプロンプト文字列になる。デフォルト値`\s-\v\$`。

|`PS2` |このパラメータの値は PS1
と同じように展開され、http://d.hatena.ne.jp/keyword/%A5%BB%A5%AB%A5%F3%A5%C0%A5%EA[セカンダリ]のプロンプト文字列として使われる。デフォルト値`>`。

|`PS3` |このパラメータの値は`select`コマンドのプロンプトとして使われる。

|`PS4`
|このパラメータは`PS1`と同じように展開される。この値は実行トレース中にhttp://d.hatena.ne.jp/keyword/bash[bash]が表示する各コマンド前に出力される。デフォルト値は`+`。

|`SHELL`
|このhttp://d.hatena.ne.jp/keyword/%B4%C4%B6%AD%CA%D1%BF%F4[環境変数]にシェルのフルパス名が保存されている。

|`TIMEFORMAT` |`time`コマンドの書式設定。 +
`%%`: 文字`%` +
`%[p][l]R`: 経過した秒数 +
`%[p][l]U`: ユーザモードで使われたCPU秒数 +
`%[p][l]S`: システムモードで使われたCPU秒数。 +
`%P`: CPUパーセンテージ `(%U + %S) / %R` +
 +
`p`: 小数点桁数 +
`l`: 分(minute)を含む +
 +
未設定時: `\nreal\t%3lR\nuser\t%3lU\nsys%3lS`

|`TMOUT`
|`0`より大きい値を設定すると、`TMOUT`の値が組込コマンド`read`のデフォルトのhttp://d.hatena.ne.jp/keyword/%A5%BF%A5%A4%A5%E0%A5%A2%A5%A6%A5%C8[タイムアウト]値となる。`select`コマンドは`TMOUT`秒入力がないと終了する。

|`TMPDIR`
|設定するとテンポラリファイル作成http://d.hatena.ne.jp/keyword/%A5%C7%A5%A3%A5%EC%A5%AF%A5%C8[ディレクト]リ名として使う。

|`auto_resume` |ユーザがジョブ制御をするときの方法を変更する。

|`histchars`
|`2`文字か`3`文字で履歴の展開とhttp://d.hatena.ne.jp/keyword/%A5%C8%A1%BC%A5%AF[トーク]ン分割を制御する。デフォルト値:
`\`^`````\'\'\'\''`。
__|===

== 対象環境

* Raspbierry pi 3 Model B+
* Raspbian stretch 9.0 2018-11-13
* http://d.hatena.ne.jp/keyword/bash[bash] 4.4.12

[source,code,lang-sh]
----
$ uname -a
Linux raspberrypi 4.14.98-v7+ #1200 SMP Tue Feb 12 20:27:48 GMT 2019 armv7l GNU/Linux
----

== 所感

　使いこなせる気がしない……。

